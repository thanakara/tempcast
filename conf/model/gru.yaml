_target_: tempcast.models.GRUForecaster

arch:
  name: gru
  units: [ 64, 32 ]
  dropout: 0.2
  recurrent_dropout: 0.1

training:
  optimizer: adam
  lr: 0.001
  epochs: 100
  batch_size: 32
  early_stopping:
    patience: 10
    monitor: val_loss
    min_delta: 0.0001

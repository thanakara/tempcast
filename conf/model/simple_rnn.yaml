_target_: railcast.models.SimpleRNNForecaster

arch:
  name: simple_rnn
  units: 32
  dropout: 0.0
  activation: tanh

training:
  optimizer: adam
  lr: 0.001
  epochs: 100
  batch_size: 32
  early_stopping:
    patience: 10
    monitor: val_loss
    min_delta: 0.0001
